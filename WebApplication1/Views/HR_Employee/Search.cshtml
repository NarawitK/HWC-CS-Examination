@model IEnumerable<WebApplication1.Models.HR_Employee>
@using System.Linq

@{
    ViewBag.Title = "ค้นหา";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ค้นหาข้อมูลพนักงาน</h2>
<div id="warningText"></div>
@using (Html.BeginForm("Search", "HR_Employee", FormMethod.Get))
{
    <div class="form-horizontal">
        <hr />
        <div class="form-group">
            @Html.Label("รหัสพนักงาน: ", htmlAttributes: new { @class = "col-md-4 control-label" })
            <div class="col-md-8">
                @Html.TextBox("EmployeeID", null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>
        @*<div class="form-group">
        @Html.Label("ชื่อพนักงาน: ", htmlAttributes: new { @class = "col-md-4 control-label" })
        <div class="col-md-8">
            @Html.TextBox("FirstName", null, htmlAttributes: new { @class = "form-control" })
        </div>
    </div>
    <div class="form-group">
        @Html.Label("นามสกุลพนักงาน: ", htmlAttributes: new { @class = "col-md-4 control-label" })
        <div class="col-md-8">
            @Html.TextBox("LastName", null, htmlAttributes: new { @class = "form-control" })
        </div>
    </div>*@
        <div class="form-group">
            @Html.Label("ชื่อ-นามสกุล พนักงาน", htmlAttributes: new { @class = "col-md-4 control-label" })
            <div class="col-md-8">
                @Html.TextBox("FullName", null, htmlAttributes: new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            @Html.Label("หน่วยงาน: ", htmlAttributes: new { @class = "col-md-4 control-label" })
            <div class="col-md-8">
                @Html.DropDownList("DepartmentID",null, "--เลือก--", htmlAttributes: new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            <input type="submit" class="col-md-offset-4 col-md-1 btn btn-default" value="Search"/>
        </div>
    </div>
}


    <table class="table table-bordered">

    @if (!Model.Any())
    {
        <tr>
            <th>
                <p class="text-warning text-center"><span class="glyphicon glyphicon-alert"></span> ไม่พบข้อมูลพนักงานที่ค้นหา</p>
            </th>
        </tr>
    }
    else
    {
        <tr>
            <th></th>
            <th>
                รหัสพนักงาน
            </th>
            <th>
                ชื่อ พนักงาน
            </th>
           <!-- <th>
                นามสกุล พนักงาน
            </th>-->
            <th>
                วันเดือนปีเกิด
            </th>
            <th>
                หัวหน้างาน
            </th>
            <th>
                วันที่แก้ไขข้อมูลล่าสุด
            </th>
            <th>
                หน่วยงานที่สังกัด
            </th>
        </tr>
        foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink("แก้ไขข้อมูล", "Edit", new { id = item.EmployeeID }) |
                    @Html.ActionLink("ดูรายละเอียด", "Details", new { id = item.EmployeeID }) |
                    @Html.ActionLink("ลบ", "Delete", new { id = item.EmployeeID })
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmployeeID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FullName)
                </td>
               <!-- <td>
                    @*Html.DisplayFor(modelItem => item.LastName)*@
                </td>-->
                <td>
                    @Html.DisplayFor(modelItem => item.Birthdate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EmployeeID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModifiedDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.HR_Department.Name)
                </td>
            </tr>
        }
    }
</table>





    



