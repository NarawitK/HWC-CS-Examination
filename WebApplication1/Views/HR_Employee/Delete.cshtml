@model WebApplication1.Models.HR_Employee

@{
    ViewBag.Title = "ลบพนักงาน";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>ลบพนักงาน</h2>

<h3>ต้องการลบข้อมูลพนักงานหรือไม่ ?</h3>
<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayName("ชื่อ-นามสกุล พนักงาน")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FullName)
        </dd>

        <dt>
            @Html.DisplayName("วัน/เดือน/ปีเกิด")
        </dt>

        <dd>
            @{
                DateTime? Bdate = Model.Birthdate;
                string FormatDate;
                if (Bdate.HasValue)
                {
                    FormatDate = Bdate.Value.ToString("dd/MM/yyyy");
                    @Html.DisplayFor(model => FormatDate);
                }
            }
        </dd>

        <dt>
            @Html.DisplayName("หัวหน้างาน")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BossID)
        </dd>

        <dt>
            @Html.DisplayName("หน่วยงานที่สังกัด")
        </dt>

        <dd>
            @Html.DisplayFor(model => model.HR_Department.Name)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-default" /> |
            @Html.ActionLink("กลับสู่หน้าหลัก", "Index")
        </div>
    }
</div>
